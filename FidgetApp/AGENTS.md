# FidgetApp — Agent Notes

**Read the repo-root `CLAUDE.md` first.** It is the authoritative orientation
for this repository (active sprint, conventions, guardrails).

This app uses Expo SDK 56. When the SDK API surface is in question, consult
the versioned docs at https://docs.expo.dev/versions/v56.0.0/ — do not rely
on memory of older SDKs (expo-av is gone; expo-audio replaced it, Reanimated
is v4 with react-native-worklets).
